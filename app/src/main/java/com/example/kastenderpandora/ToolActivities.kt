package com.example.kastenderpandora

class LightActivity : BaseToolActivity() {
    override val toolTitle = R.string.light
}

class RulerActivity : BaseToolActivity() {
    override val toolTitle = R.string.ruler
}

class CounterActivity : BaseToolActivity() {
    override val toolTitle = R.string.counter
}

class ClockActivity : BaseToolActivity() {
    override val toolTitle = R.string.clock
}

class CalculatorActivity : BaseToolActivity() {
    override val toolTitle = R.string.calculator
}

class ProtractorActivity : BaseToolActivity() {
    override val toolTitle = R.string.protractor
}

class SpiritLevelActivity : BaseToolActivity() {
    override val toolTitle = R.string.spirit_level
}

class DecibelMeterActivity : BaseToolActivity() {
    override val toolTitle = R.string.decibel_meter
}

class CompassActivity : BaseToolActivity() {
    override val toolTitle = R.string.compass
}

class SirenActivity : BaseToolActivity() {
    override val toolTitle = R.string.siren
}

class MagnifyingGlassActivity : BaseToolActivity() {
    override val toolTitle = R.string.magnifying_glass
}

class VoiceRecorderActivity : BaseToolActivity() {
    override val toolTitle = R.string.voice_recorder
}

class LumenMeterActivity : BaseToolActivity() {
    override val toolTitle = R.string.lumen_meter
}

class CameraActivity : BaseToolActivity() {
    override val toolTitle = R.string.camera
}

class AudioSpectrumActivity : BaseToolActivity() {
    override val toolTitle = R.string.audio_spectrum
}
